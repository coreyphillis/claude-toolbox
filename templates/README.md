# Templates

Reusable starting points shared across repos — project scaffolds, config
files, and boilerplate. Copy what you need into a new project.

Add a new template as its own subdirectory with a short note here describing
what it's for.
